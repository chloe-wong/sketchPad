# Used by Vite at build time for production builds only.
# Empty string = use relative URLs, which Nginx proxies to the backend.
# Do not change this unless you know what you're doing.
VITE_API_BASE=
